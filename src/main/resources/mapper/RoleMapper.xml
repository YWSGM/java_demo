<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.RoleMapper">
    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.example.demo.entity.Role">
        <id column="role_id" property="roleId" jdbcType="INTEGER"/>
        <result column="role_name" property="roleName" jdbcType="VARCHAR"/>
        <result column="role_desc" property="roleDesc" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 公共列 -->
    <sql id="Base_Column_List">
        role_id, role_name, role_desc, status, create_time, update_time
    </sql>

    <!--根据ID查询角色-->
    <select id="findByRoleId" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        SELECT
            <include refid="Base_Column_List"/>
        FROM roles
        WHERE role_id = #{id}
    </select>

    <!--查询角色列表-->
    <select id="getRoleList" resultMap="BaseResultMap">
        SELECT
            <include refid="Base_Column_List"/>
        FROM roles
    </select>

    <!--创建角色-->
    <insert id="createRole"  parameterType="com.example.demo.entity.Role" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO roles (role_name, role_desc, status)
        VALUES (#{role_name}, #{role_desc}, #{status})
    </insert>
</mapper>